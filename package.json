{
  "name": "hrm-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "concurrently -c \"blue,magenta\" \"pnpm db:up\" \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter api build && pnpm --filter web build",
    "start": "pnpm db:up && pnpm --filter api start & pnpm --filter web preview",
    "lint": "pnpm --filter api lint && pnpm --filter web lint",
    "db:up": "docker-compose up -d db",
    "db:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}
